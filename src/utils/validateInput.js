/* Completar esta funcion debe devolver true si es valido el input o false cuando no
- exactLength (int) --> El largo exacto (sacando espacios) de text debe ser igual al parámetro
- minLength (int) --> el largo de text (espacios incluidos) debe ser igual o mayor que el valor que contiene la regla
- minWords (int) --> la cantidad de palabras de text (separadas por espacios, comas o puntos) debe ser mayor o igual que el valor del parametro
- maxLength (int) --> el largo de text (espacios incluidos) debe ser igual o menor que el valor que contiene la regla
- maxWords (int) --> la cantidad de palabras de text (separadas por espacios, comas o puntos) debe ser menor o igual que el valor del parametro
- containsChars (array) --> text debe incluir a todos los caracteres en el array
*/
export const validityChecker = (text, validations) => {
    return true 
}